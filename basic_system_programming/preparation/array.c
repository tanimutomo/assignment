#include <stdio.h>

int main(void) {
    int array[4];

    array[0] = 2;
    array[1] = 3;
    array[2] = 5;
    array[3] = 7;

    printf("%d, %d, %d, %d\n", array[0], array[1], array[2], array[3]);

    return 0;
}
